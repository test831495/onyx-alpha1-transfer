const labels: Record<string, string> = { SESSION_ACTIVE: "Session active", SESSION_EXPIRED_INACTIVITY: "Session ended because it was inactive", SESSION_EXPIRED_ABSOLUTE: "Session time limit reached", SESSION_REVOKED: "This session has ended", SESSION_REPLACED: "A newer session is now active", SESSION_ROTATION_REQUIRED: "Session refresh required", STEP_UP_REQUIRED: "Please verify your identity to continue", INVALID_SESSION_TIME: "This session cannot be verified", ACCOUNT_SWITCH_REQUIRED: "Please switch to the correct account", AUDIT_UNAVAILABLE: "This protected action is temporarily unavailable", SHARED_DEVICE_RESTRICTED: "This action is unavailable on a shared device" };
export function friendlySessionLabel(code: string): string { return labels[code] ?? "This session cannot be verified"; }
export { labels as SESSION_LABELS };
