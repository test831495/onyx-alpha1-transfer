import{describe,it,expect}from"vitest";import{evaluateProviderHealth,validateConfiguration,type OnyxConfiguration}from"./index";
describe("configuration runtime",()=>{it("never returns secret values",()=>{const value=evaluateProviderHealth("speech",{enabled:true,role:"voice",publicEnv:["REGION"],secretEnv:["KEY"]},{get:n=>n==="REGION"?"centralindia":"super-secret"});expect(value.state).toBe("configured");expect(JSON.stringify(value)).not.toContain("super-secret")});it("reports missing values",()=>{const value=evaluateProviderHealth("speech",{enabled:true,role:"voice",publicEnv:["REGION"],secretEnv:["KEY"]},{get:()=>undefined});expect(value.state).toBe("unconfigured");expect(value.missing).toEqual(["REGION","KEY"])});it("blocks prototype production enablement",()=>{const c={version:"1",environment:"development",production:{deploymentAllowed:true,liveNetlifyUpdatesAllowed:false},providers:{},routing:{},policies:{}} as OnyxConfiguration;expect(validateConfiguration(c)).toContain("production deployment must remain blocked during prototype development")})});
