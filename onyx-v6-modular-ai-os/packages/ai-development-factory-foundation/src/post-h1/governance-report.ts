export type GovernanceReport = Readonly<{ currentState: string; currentGate: string; acceptedMarkers: readonly unknown[]; targetLock: string; lineage: readonly unknown[]; acceptanceSummary: string; findingSummary: string; evidenceFreshnessSummary: string; knownLimitations: readonly unknown[]; residualRisks: readonly unknown[]; authorityBoundaries: readonly unknown[]; mergeReadiness: string; mainClosure: string; nextGate: string; reopeningTriggers: readonly unknown[]; verificationOutcome: string; reasonCodes: readonly unknown[]; authority: "NON_AUTHORIZING" }>;

export const projectGovernanceReport = (input: Omit<GovernanceReport, "authority">): GovernanceReport => Object.freeze({ ...input, acceptedMarkers: Object.freeze([...input.acceptedMarkers]), lineage: Object.freeze([...input.lineage]), knownLimitations: Object.freeze([...input.knownLimitations]), residualRisks: Object.freeze([...input.residualRisks]), authorityBoundaries: Object.freeze([...input.authorityBoundaries]), reopeningTriggers: Object.freeze([...input.reopeningTriggers]), reasonCodes: Object.freeze([...input.reasonCodes]), authority: "NON_AUTHORIZING" });