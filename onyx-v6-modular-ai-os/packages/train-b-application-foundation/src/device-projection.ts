export const DEVICE_CLASSES=Object.freeze(["WEB","DESKTOP","MOBILE","TABLET","TV","FUTURE_XR"] as const); export type DeviceClass=(typeof DEVICE_CLASSES)[number];
export interface DeviceProjection { readonly device:DeviceClass; readonly detailLevel:"COMPACT"|"STANDARD"|"DETAILED"; readonly maxVisibleItems:number; readonly safeZone:string; readonly reducedMotion:boolean; readonly privacyRedaction:"NONE"|"REDUCED"|"STRICT"; readonly nonAuthorizing:true; }
export function createDeviceProjection(device:DeviceClass, sharedRoom=false):DeviceProjection { if(!DEVICE_CLASSES.includes(device)) throw new Error("Unsupported device"); return Object.freeze({device,detailLevel:device === "TV" ? "COMPACT" : "STANDARD",maxVisibleItems:device === "MOBILE" ? 8 : 24,safeZone:device === "TV" ? "TV_SAFE" : "DEFAULT",reducedMotion:false,privacyRedaction:sharedRoom ? "STRICT" : "NONE",nonAuthorizing:true as const}); }