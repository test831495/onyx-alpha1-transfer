import type { ConnectorHealthFacts, ConnectorRegistration, RuntimeState } from "./model";
export function projectRuntimeState(connector: ConnectorRegistration, facts: ConnectorHealthFacts): RuntimeState { const blocked = ["DISABLED", "REVOKED", "EXPIRED", "QUARANTINED", "REMOVED", "UNKNOWN"].includes(connector.lifecycleState); const healthBlocked = facts.lifecycleState === "UNKNOWN" || facts.credentialReferenceAvailable === false; return Object.freeze({ connectorId: connector.id, state: blocked ? connector.lifecycleState : healthBlocked ? "UNKNOWN" : connector.lifecycleState, eligibleCapabilityIds: Object.freeze(blocked || healthBlocked ? [] : connector.bindings.map((binding) => binding.capabilityId)), reasons: Object.freeze(blocked ? [`CONNECTOR_${connector.lifecycleState}`] : healthBlocked ? ["POLICY_FACT_UNKNOWN"] : []) }); }
export const requestEnablement = (connector: ConnectorRegistration) => Object.freeze({ connectorId: connector.id, proposal: "ENABLE", authorizing: false, eligible: !["REVOKED", "REMOVED", "EXPIRED", "QUARANTINED"].includes(connector.lifecycleState), reason: ["REVOKED", "REMOVED", "EXPIRED", "QUARANTINED"].includes(connector.lifecycleState) ? "EXTERNAL_POLICY_REQUIRED" : "PROPOSAL_ONLY" });
export const requestDisablement = (connector: ConnectorRegistration, reason: string) => Object.freeze({ connectorId: connector.id, proposal: "DISABLE", reason, authorizing: false });