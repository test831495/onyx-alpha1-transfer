import type { ConnectorFreshnessEvaluation, ConnectorHealthEvaluation, ConnectorHealthFacts } from "./model";
export function evaluateConnectorHealth(facts: ConnectorHealthFacts): ConnectorHealthEvaluation { const reasons: string[] = []; if (["DISABLED", "REVOKED", "QUARANTINED", "EXPIRED"].includes(facts.lifecycleState)) return Object.freeze({ connectorId: facts.connectorId, state: facts.lifecycleState === "DISABLED" ? "DISABLED" : facts.lifecycleState === "REVOKED" ? "REVOKED" : facts.lifecycleState === "EXPIRED" ? "EXPIRED" : "QUARANTINED", reasonCodes: Object.freeze([`LIFECYCLE_${facts.lifecycleState}`]), assessable: true }); if (facts.rateLimited === true) return Object.freeze({ connectorId: facts.connectorId, state: "RATE_LIMITED", reasonCodes: Object.freeze(["RATE_LIMITED"]), assessable: true }); if (facts.permissionReduced === true) return Object.freeze({ connectorId: facts.connectorId, state: "PERMISSION_REDUCED", reasonCodes: Object.freeze(["PERMISSION_REDUCED"]), assessable: true }); if (facts.lifecycleState === "UNKNOWN" || facts.credentialReferenceAvailable === undefined || facts.adapterHealthReference === undefined) reasons.push("HEALTH_FACT_UNKNOWN"); else if (facts.consecutiveFailures && facts.consecutiveFailures > 0) return Object.freeze({ connectorId: facts.connectorId, state: "DEGRADED", reasonCodes: Object.freeze(["CONSECUTIVE_FAILURES"]), assessable: true }); else return Object.freeze({ connectorId: facts.connectorId, state: "CONNECTED", reasonCodes: Object.freeze([]), assessable: true }); return Object.freeze({ connectorId: facts.connectorId, state: "UNKNOWN", reasonCodes: Object.freeze(reasons), assessable: false }); }
export function evaluateFreshness(facts: ConnectorHealthFacts): ConnectorFreshnessEvaluation { if (facts.freshnessObserved === true) return Object.freeze({ connectorId: facts.connectorId, state: "CURRENT", assessable: true, reasonCodes: Object.freeze([]) }); if (facts.freshnessObserved === false) return Object.freeze({ connectorId: facts.connectorId, state: "STALE", assessable: true, reasonCodes: Object.freeze(["OBSERVATION_STALE"]) }); return Object.freeze({ connectorId: facts.connectorId, state: "UNKNOWN", assessable: false, reasonCodes: Object.freeze(["FRESHNESS_UNKNOWN"]) }); }