export const ACCEPTANCE_IDS = Object.freeze(["REG", "ACCOUNT", "BIND", "HEALTH", "STATE", "VAULT", "ATTR", "COST", "RECOVERY", "SCOPE"].flatMap((family) => Array.from({ length: family === "REG" || family === "HEALTH" || family === "SCOPE" ? 12 : family === "ACCOUNT" || family === "BIND" || family === "STATE" || family === "RECOVERY" ? 10 : 8 }, (_, index) => `TB-CON-${family}-${String(index + 1).padStart(3, "0")}`)));
export function validateAcceptanceRegistry() { const ids = new Set(ACCEPTANCE_IDS); return Object.freeze({ valid: ids.size === 100 && ACCEPTANCE_IDS.length === 100, count: ACCEPTANCE_IDS.length, ids: ACCEPTANCE_IDS }); }