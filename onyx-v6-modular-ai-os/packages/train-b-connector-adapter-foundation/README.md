# Train B Connector Adapter Foundation

Package 6A provides the provider-neutral ConnectorAdapter runtime boundary and deterministic provider-adapter conformance kit.

It is configuration-driven, account-bound, vault-reference-only, read-only, non-authorizing, and independent of provider SDKs and network clients. Provider adapters are not implemented here.
