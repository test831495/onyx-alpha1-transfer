import{describe,it,expect}from"vitest";import{createApproval,createJob,ExecutionCoordinator,InMemoryJobStore}from"@onyx/automation-foundation";import{GitHubDryRunAdapter,planIssue}from"../src/index";describe("GitHub dry-run adapter",()=>{it("never mutates GitHub",async()=>{const p=planIssue("r",{title:"x",body:"y"});const j=createJob(p);j.approval=createApproval(p,"Rahul");const result=await new ExecutionCoordinator(new InMemoryJobStore(),new GitHubDryRunAdapter()).run(j);expect(result.remoteMutationPerformed).toBe(false);expect((result.result as { simulated: boolean }).simulated).toBe(true)});it("rejects missing approval",async()=>{const p=planIssue("r",{title:"x",body:"y"});const j=createJob(p);await expect(new ExecutionCoordinator(new InMemoryJobStore(),new GitHubDryRunAdapter()).run(j)).rejects.toThrow("approval")})});
