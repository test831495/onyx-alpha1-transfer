import{describe,it,expect}from"vitest";import{planIssue,planBranch,planDraftPullRequest,idempotencyKey}from"../src/index";const repo="test831495/onyx-alpha1-transfer";describe("GitHub dry-run planners",()=>{it("creates approval-gated issue plan",()=>{const p=planIssue(repo,{title:"UX",body:"body"});expect(p.dryRun).toBe(true);expect(p.approvalRequired).toBe(true)});it("validates branch names",()=>{expect(()=>planBranch(repo,{name:"Bad Branch",from:"main"})).toThrow();expect(planBranch(repo,{name:"feature/valid-branch",from:"integration/onyx-nova"}).workingBranch).toBe("feature/valid-branch")});it("rejects PRs targeting main",()=>expect(()=>planDraftPullRequest(repo,{title:"x",body:"x",base:"main",head:"feature/x",draft:true})).toThrow());it("creates Draft PR plan",()=>expect(planDraftPullRequest(repo,{title:"x",body:"x",base:"integration/onyx-nova",head:"feature/x",draft:true}).capabilityId).toBe("github.pr.create-draft"));it("creates stable idempotency keys",()=>expect(idempotencyKey(repo,"issue",{a:1})).toBe(idempotencyKey(repo,"issue",{a:1}))) });
