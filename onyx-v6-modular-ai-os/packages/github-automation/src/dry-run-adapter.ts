import type{AutomationJob,DryRunExecutor}from"@onyx/automation-foundation";import type{GitHubDryRunResult}from"./contracts";export class GitHubDryRunAdapter implements DryRunExecutor{async execute(job:AutomationJob):Promise<{remoteMutationPerformed:false;result:GitHubDryRunResult<unknown>}>{return{remoteMutationPerformed:false,result:{operation:job.plan.capabilityId,simulated:true,remoteMutationPerformed:false,approvalRequired:job.plan.approvalRequired,payload:job.plan.payload,warnings:["DRY RUN ONLY","No GitHub remote mutation was performed."]}}}}
