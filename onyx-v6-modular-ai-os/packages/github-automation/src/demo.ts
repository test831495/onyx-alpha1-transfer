import{InMemoryJobStore,ExecutionCoordinator,createApproval,createJob,buildEvidence}from"@onyx/automation-foundation";import{GitHubDryRunAdapter,planIssue,planBranch,planDraftPullRequest}from"./index";const repository="test831495/onyx-alpha1-transfer";const plans: import("@onyx/automation-foundation").ExecutionPlan<unknown>[]=[planIssue(repository,{title:"Phase 1A.1e UX backlog",body:"Track remaining UX acceptance and polish."}),planBranch(repository,{name:"feature/phase1a1e-ux-followup",from:"feature/phase1a2-github-automation-foundation"}),planDraftPullRequest(repository,{title:"Phase 1A.1e UX Follow-up",body:"Draft only",base:"feature/phase1a2-github-automation-foundation",head:"feature/phase1a1e-ux-followup",draft:true})];for(const plan of plans){const job=createJob(plan);job.approval=createApproval(plan,"Rahul Kumar");const store=new InMemoryJobStore();const coordinator=new ExecutionCoordinator(store,new GitHubDryRunAdapter());const outcome=await coordinator.run(job);console.log(JSON.stringify({plan,outcome,evidence:buildEvidence(store.get(job.id)!,"ALLOW_WITH_APPROVAL")},null,2))}
