import{createApproval,createJob}from"@onyx/automation-foundation";import{planIssue,idempotencyKey}from"./index";const plan=planIssue("test831495/onyx-alpha1-transfer",{title:"Phase 1A.2C approval-gated write demo",body:"Dry preview only unless an explicit execution workflow is used."});const approval=createApproval(plan,"Rahul Kumar");console.log(JSON.stringify({plan,approval,idempotencyKey:idempotencyKey(plan.repository,plan.capabilityId,plan.payload),execute:false,remoteMutationPerformed:false,warning:"This demo does not execute a live write."},null,2));
