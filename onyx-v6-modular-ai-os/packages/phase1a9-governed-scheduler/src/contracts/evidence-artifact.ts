import { PHASE1A9_EVIDENCE_ARTIFACT_CONTRACT_VERSION } from "../shared/versions";
export interface EvidenceArtifactRecord { artifactId: string; fileName: string; producerComponent: string; format: string; schemaVersion: string; requiredFields: readonly string[]; hashingStrategy: string; provenanceRequirement: string; validationMethod: string; releaseRequirement: string; implementationWave: string; status: "REGISTERED" | "NOT_STARTED"; contractVersion: string; }
export function assertEvidenceArtifactRecord(record: EvidenceArtifactRecord): void { if (record.contractVersion !== PHASE1A9_EVIDENCE_ARTIFACT_CONTRACT_VERSION || !record.artifactId || !record.fileName || !["REGISTERED", "NOT_STARTED"].includes(record.status)) throw new Error("Invalid evidence artifact record."); }