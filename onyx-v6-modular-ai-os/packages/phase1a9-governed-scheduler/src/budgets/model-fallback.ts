import type { ModelRoutingClass } from "@onyx/phase1a8-governed-contracts";
export interface ModelFallbackDecision { fallbackDecisionId: string; selectedModelClass: ModelRoutingClass | null; fallbackModelClasses: readonly ModelRoutingClass[]; decision: "FALLBACK_REQUIRED" | "FAILED_SAFE" | "PROHIBITED"; reason: string; evidenceArtifactIds: readonly string[]; evaluatedAt: string; contractVersion: string; }
export function evaluateModelFallback(input: { fallbackDecisionId: string; allowedClasses: readonly ModelRoutingClass[]; fallbackOrder: readonly ModelRoutingClass[]; tokenHardStop: boolean; costHardStop: boolean; apiCallHardStop: boolean; privacyCompatible: boolean; approvalRequired: boolean; evidenceArtifactIds: readonly string[]; evaluatedAt: string; contractVersion: string }): ModelFallbackDecision { const order: ModelRoutingClass[] = ["LOCAL_SMALL", "LOCAL_MEDIUM", "CLOUD_SMALL", "CLOUD_MEDIUM", "CLOUD_PREMIUM"]; const candidates = [...input.fallbackOrder, ...order].filter((value, index, values) => values.indexOf(value) === index && input.allowedClasses.includes(value)); const blocked = input.tokenHardStop || input.costHardStop || input.apiCallHardStop || !input.privacyCompatible || input.approvalRequired; return { fallbackDecisionId: input.fallbackDecisionId, selectedModelClass: blocked ? null : candidates[0] ?? null, fallbackModelClasses: blocked ? [] : candidates.slice(1), decision: blocked ? (input.approvalRequired ? "PROHIBITED" : "FAILED_SAFE") : candidates.length ? "FALLBACK_REQUIRED" : "FAILED_SAFE", reason: blocked ? "governance-gate" : candidates.length ? "deterministic-fallback-order" : "no-compatible-fallback", evidenceArtifactIds: input.evidenceArtifactIds, evaluatedAt: input.evaluatedAt, contractVersion: input.contractVersion }; }
