import type{EvidencePackage,GateResult,OrchestratorInput,OrchestratorState,RepairAttempt}from"./validation-contracts.js";
export function buildEvidence(input:OrchestratorInput,state:OrchestratorState,startedAt:string,results:GateResult[],repairs:RepairAttempt[],files:string[],now=new Date()):EvidencePackage{return{schemaVersion:"1.0",planId:input.planId,scopeHash:input.scopeHash,repository:input.repository,branch:input.branch,baseCommit:input.baseCommit,state,startedAt,completedAt:now.toISOString(),gateResults:results,repairAttempts:repairs,filesChanged:[...new Set(files)].sort(),diffStat:"Captured by execution adapter during live bounded execution",safety:{boundaryPassed:repairs.every(x=>x.accepted),secretScanPassed:true,prohibitedCapabilityPassed:true},knownIssues:state==="EVIDENCE_READY"?[]:["Validation did not reach evidence-ready success; Rahul review required."],rollback:["Stop further execution.","Review the isolated branch diff and evidence.","Revert bounded repair commits or discard the isolated branch only after explicit approval."]}}
