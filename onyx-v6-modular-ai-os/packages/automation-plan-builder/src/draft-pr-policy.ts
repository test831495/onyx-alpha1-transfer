import type{DraftPrApproval,DraftPrPackage}from"./draft-pr-contracts.js";
export function validateDraftApproval(pkg:DraftPrPackage,a:DraftPrApproval|undefined,now=new Date()){const v:string[]=[];if(!a)v.push("Draft PR creation approval is required");else{if(a.approvedBy!=="Rahul Kumar")v.push("Approval authority is not Rahul Kumar");if(a.reason.trim().length<8)v.push("Approval reason must contain at least 8 characters");if(a.scopeHash!==pkg.scopeHash)v.push("Approval scope hash mismatch");if(a.expiresAt&&new Date(a.expiresAt).getTime()<=now.getTime())v.push("Approval has expired")}if(!pkg.draft)v.push("PR must be draft");if(pkg.mergeAllowed)v.push("Merge must remain prohibited");if(pkg.productionDeployAllowed)v.push("Production deployment must remain prohibited");return{allowed:v.length===0,violations:v}}
