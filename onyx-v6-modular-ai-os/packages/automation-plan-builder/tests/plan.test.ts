import{describe,it,expect}from"vitest";import{buildPlan}from"../src/index.js";const i={repository:"test831495/onyx-alpha1-transfer",issueNumber:5,title:"Plan work",body:"- [ ] Parse criteria\n- [ ] Require approval",baseBranch:"feature/phase1a2-github-automation-foundation",requestedBy:"Rahul Kumar"};describe("plan",()=>{it("is deterministic and gated",()=>{const a=buildPlan(i,new Date(0)),b=buildPlan(i,new Date(1));expect(a.scopeHash).toBe(b.scopeHash);expect(a.status).toBe("AWAITING_SCOPE_APPROVAL");expect(a.acceptanceCriteria).toHaveLength(2)});it("blocks protected bases",()=>expect(()=>buildPlan({...i,baseBranch:"main"})).toThrow(/Protected/));it("blocks merge and production",()=>{const p=buildPlan(i);expect(p.prohibitedCapabilities).toContain("merge pull request");expect(p.prohibitedCapabilities).toContain("deploy production")});it("classifies sensitive scope",()=>expect(buildPlan({...i,title:"Change secret permissions"}).risk.level).toBe("critical"))});