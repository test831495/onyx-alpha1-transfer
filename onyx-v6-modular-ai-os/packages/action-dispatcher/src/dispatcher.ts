import type {ActionResult,Intent} from "@onyx/contracts";import {failedResult,unsupportedResult} from "./result";import type {ActionContext,ActionHandlerRegistry} from "./registry";
export class ActionDispatcher{constructor(private readonly registry:ActionHandlerRegistry){}async dispatch(intent:Intent,context:ActionContext):Promise<ActionResult>{if(context.signal.aborted)return{status:"cancelled",message:"Action cancelled before execution."};const handler=this.registry.get(intent.kind);if(!handler)return unsupportedResult("NO_HANDLER",`No handler registered for ${intent.kind}`);try{return await handler(intent,context);}catch(error){return failedResult("HANDLER_FAILURE",error instanceof Error?error.message:"Unknown action failure.");}}}
