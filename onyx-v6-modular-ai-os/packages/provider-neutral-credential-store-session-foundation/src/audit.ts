export type RefreshErrorClass = "invalid_grant" | "http_401" | "rate_limited_429" | "server_error_5xx" | "network" | "malformed";
export type AuditEvent = { readonly event: string; readonly recordId: string; readonly canonicalAccountRef: string; readonly providerId?: string; readonly purpose?: string; readonly errorClass?: RefreshErrorClass; readonly at: string };
export function classifyRefreshError(error: unknown): RefreshErrorClass { const text = error instanceof Error ? `${error.name} ${error.message}`.toLowerCase() : String(error).toLowerCase(); if (text.includes("invalid_grant")) return "invalid_grant"; if (text.includes("401") || text.includes("unauthorized")) return "http_401"; if (text.includes("429") || text.includes("rate limit")) return "rate_limited_429"; if (/\b5\d\d\b/.test(text) || text.includes("server error")) return "server_error_5xx"; if (text.includes("timeout") || text.includes("econn") || text.includes("network")) return "network"; return "malformed"; }
export class PrivacySafeAuditSink { private readonly events: AuditEvent[] = []; record(event: Omit<AuditEvent, "at">): void { this.events.push({ ...event, at: new Date().toISOString() }); } getEvents(): readonly AuditEvent[] { return [...this.events]; } }