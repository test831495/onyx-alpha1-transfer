import type{Handler}from'@netlify/functions';export const handler:Handler=async event=>{if(event.httpMethod!=='POST')return{statusCode:405,body:'Method not allowed'};const body=JSON.parse(event.body||'{}');return{statusCode:200,headers:{'content-type':'application/json'},body:JSON.stringify({requestId:crypto.randomUUID(),accepted:true,route:body.mode==='nova'?'local':'cloud',message:'Gateway scaffold accepted command'})}};