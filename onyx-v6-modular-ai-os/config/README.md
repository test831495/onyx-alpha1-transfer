# ONYX Configuration Hub

`onyx.config.json` stores non-secret settings and environment-variable names only. Actual credentials remain in Codespaces/Actions secrets. Never add API keys to this directory or use `VITE_` for sensitive values.
