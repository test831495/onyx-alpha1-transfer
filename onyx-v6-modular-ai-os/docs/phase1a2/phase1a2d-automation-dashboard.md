# Phase 1A.2D Automation Dashboard and Approval Center

The first UI release is supervisory. It visualizes jobs, local approval decisions, evidence, audit milestones, recovery tags, and GitHub capability health. It does not execute remote writes. Approve and Reject update only local component state; Execute is disabled.
