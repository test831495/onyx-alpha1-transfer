import manifest from '../manifest.json';import type{OnyxPlugin,PluginContext}from'@onyx/plugin-sdk';export const plugin:OnyxPlugin={manifest,async execute(tool,input,context){context.emit('plugin.started',{plugin:manifest.id,tool});return{ok:true,plugin:manifest.id,tool,input,status:'scaffold: connect provider adapter'}},async health(){return{ok:true,detail:'manifest loaded; provider not configured'}}};