import type{AssistantMode,CoreState}from'@onyx/contracts';export function CoreOrb({mode,state,onMode}:{mode:AssistantMode;state:CoreState;onMode:(m:AssistantMode)=>void}){return <div className={`core-zone ${mode} ${state}`}><div className="orbit o1"/><div className="orbit o2"/><button className="ai-core" onClick={()=>onMode(mode==='nova'?'onyx':'nova')} aria-label={`Switch from ${mode}`}><span>{mode.toUpperCase()}</span></button><strong>{state.replace('-',' ').toUpperCase()}</strong><small>Say “Hey {mode==='nova'?'Nova':'Onyx'}” or tap the core</small></div>}