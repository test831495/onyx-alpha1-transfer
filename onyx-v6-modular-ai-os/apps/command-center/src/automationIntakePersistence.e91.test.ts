import{beforeEach,describe,it,expect}from"vitest";import{createAutomationIssueDraft,deleteAutomationIssueDraft,listAutomationIssueDrafts,loadActiveAutomationIssueDraft,saveAutomationIssueDraft}from"./automationIntakeService";const input={goal:"Persist this governed automation draft across reloads",repository:"test831495/onyx-alpha1-transfer",baseBranch:"feature/phase1a2-github-automation-foundation",requestedBy:"Rahul Kumar" as const,allowedPaths:["apps/command-center/src"]};describe("E.9.1 persistence",()=>{beforeEach(()=>localStorage.clear());it("saves and reloads active draft",()=>{const d=createAutomationIssueDraft(input,new Date(0));saveAutomationIssueDraft(d);expect(listAutomationIssueDrafts()).toHaveLength(1);expect(loadActiveAutomationIssueDraft()?.scopeHash).toBe(d.scopeHash)});it("deduplicates same scope and deletes safely",()=>{const d=createAutomationIssueDraft(input,new Date(0));saveAutomationIssueDraft(d);saveAutomationIssueDraft(d);expect(listAutomationIssueDrafts()).toHaveLength(1);deleteAutomationIssueDraft(d.draftId);expect(listAutomationIssueDrafts()).toHaveLength(0)});it("retains no-execution governance",()=>{const d=createAutomationIssueDraft(input,new Date(0));expect(d.remoteIssueCreated).toBe(false);expect(d.executionStarted).toBe(false);expect(d.mergeAllowed).toBe(false)})});
