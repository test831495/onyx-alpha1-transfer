import{describe,it,expect}from"vitest";import{loadAutomationSnapshot}from"../automationDashboardData";describe("E.8A automation UI data",()=>{it("loads E.5-style snapshot",()=>{const s=loadAutomationSnapshot();expect(s.source).toBe("E5_DASHBOARD_SERVICE");expect(s.jobs[0]?.issueNumber).toBe(5);expect(s.jobs[0]?.state).toBe("DRAFT_PR_CREATED");expect(s.jobs[0]?.draftPr?.number).toBe(6)});it("keeps merge and production unavailable",()=>{const s=loadAutomationSnapshot();expect(s.mergeAllowed).toBe(false);expect(s.productionDeployAllowed).toBe(false)})});