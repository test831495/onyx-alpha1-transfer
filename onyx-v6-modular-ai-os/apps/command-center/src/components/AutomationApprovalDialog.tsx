import{useMemo,useState,type CSSProperties}from"react";import{createApprovalDecision,validateApprovalDecision}from"../automationApprovalService";import type{ApprovalAction,ApprovalDecision}from"../automationApprovalContracts";
const shell:CSSProperties={position:"fixed",inset:0,zIndex:13000,display:"grid",placeItems:"center",background:"rgba(0,8,18,.76)"};const panel:CSSProperties={width:"min(620px,calc(100vw - 32px))",padding:20,borderRadius:16,border:"1px solid #2e8aa5",background:"#071a2f",color:"#e8fbff",boxShadow:"0 20px 70px rgba(0,0,0,.55)"};const field:CSSProperties={width:"100%",boxSizing:"border-box",padding:10,borderRadius:9,border:"1px solid #34768c",background:"#041224",color:"#effcff"};const button:CSSProperties={padding:"9px 13px",borderRadius:9,border:"1px solid #3c91aa",background:"#0b5268",color:"white",fontWeight:700,cursor:"pointer"};
export function ApprovalDialog({action,issueNumber,scopeHash,onCancel,onSubmit}:{action:ApprovalAction;issueNumber:number;scopeHash:string;onCancel:()=>void;onSubmit:(x:ApprovalDecision)=>void}){const[reason,setReason]=useState(""),[expiresAt,setExpiresAt]=useState("");const gate=useMemo(()=>validateApprovalDecision({action,approvedBy:"Rahul Kumar",reason,scopeHash,expectedScopeHash:scopeHash,expiresAt:expiresAt||undefined}),[action,reason,scopeHash,expiresAt]);return <div role="dialog" aria-modal="true" aria-label="Governed automation approval" style={shell}><div style={panel}><small>GOVERNED DECISION · LOCAL AUDIT EVENT</small><h3>{action.replaceAll("_"," ")}</h3><p>Issue #{issueNumber}</p><label>Approver<input style={field} value="Rahul Kumar" readOnly/></label><br/><label>Exact scope hash<input style={field} value={scopeHash} readOnly/></label><br/><label>Meaningful reason<textarea style={{...field,minHeight:90}} value={reason} onChange={e=>setReason(e.target.value)} placeholder="Explain why this decision is appropriate"/></label><br/><label>Expiry, optional<input style={field} type="datetime-local" value={expiresAt} onChange={e=>setExpiresAt(e.target.value)}/></label>{!gate.allowed&&<p style={{color:"#ffb0b0"}}>{gate.violations.join(" · ")}</p>}<p style={{color:"#ffd166"}}>No GitHub write, merge, production deployment, permission change, or secret operation will occur.</p><div style={{display:"flex",justifyContent:"flex-end",gap:8}}><button style={{...button,background:"#10293e"}} onClick={onCancel}>Cancel</button><button style={{...button,opacity:gate.allowed?1:.45}} disabled={!gate.allowed} onClick={()=>onSubmit(createApprovalDecision({action,reason,scopeHash,issueNumber,expiresAt:expiresAt||undefined}))}>Record decision</button></div></div></div>}
