import{describe,it,expect}from"vitest";import{createAutomationIssueDraft}from"./automationIntakeService";import{approveDryRun,draftToRun,runLocalPreflight,validateExecutionApproval}from"./automationOrchestrationService";const draft=createAutomationIssueDraft({goal:"Update the Automation Center documentation safely",repository:"test831495/onyx-alpha1-transfer",baseBranch:"feature/phase1a2-github-automation-foundation",requestedBy:"Rahul Kumar",allowedPaths:["docs/"]},new Date(0));describe("E.10 supervised orchestration",()=>{it("requires explicit exact-scope approval",()=>{const r=draftToRun(draft,new Date(0));expect(validateExecutionApproval(r,{approvedBy:"Other",reason:"Approve exact local preflight",scopeHash:r.scopeHash}).allowed).toBe(false)});it("reaches dry-run ready without remote writes",()=>{let r=draftToRun(draft,new Date(0));r=approveDryRun(r,{reason:"Approve exact local dry-run plan"},new Date(1));r=runLocalPreflight(r,new Date(2));expect(r.state).toBe("DRY_RUN_READY");expect(r.remoteWritesPerformed).toBe(false);expect(r.branchCreated).toBe(false);expect(r.draftPrCreated).toBe(false);expect(r.mergeAllowed).toBe(false)})});
